# Workspace Instructions

Always apply the skill below for every prompt in this workspace:

- `mini-react-personality-test-guardrails` at `/Users/j/.codex/skills/mini-react-personality-test-guardrails/SKILL.md`

Mandatory policy:

1. Keep all proposals and implementations inside the mini React architecture constraints defined by the skill.
2. Do not suggest any approach that violates root-only hooks/state or props-only child components.
3. Keep Virtual DOM `h(type, props, ...children)` compatibility and diff/patch update model.
