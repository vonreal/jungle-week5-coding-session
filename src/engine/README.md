# Engine Workspace

이 폴더는 엔진 개발팀 작업 영역입니다.

- `core/`: FunctionComponent, hook runtime
- `vdom/`: Virtual DOM, diff, patch
- `shared/`: 엔진과 앱이 함께 쓰는 공통 타입/계약

페이지 팀은 이 폴더의 구현 세부사항을 직접 수정하지 않는 것을 기본 원칙으로 합니다.
