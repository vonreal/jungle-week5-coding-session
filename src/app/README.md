# App Workspace

이 폴더는 성향 테스트 페이지 팀 작업 영역입니다.

- `components/`: props-only stateless component
- `data/`: 질문/결과 데이터
- `styles/`: 앱 스타일
- `bootstrap.js`: 엔진과 앱 연결 시작점

앱 팀은 자식 컴포넌트 내부에 state를 두지 않고, 루트 `App` 기준 구조를 유지합니다.
